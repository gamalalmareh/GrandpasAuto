version: 1

frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm ci
    build:
      commands:
        - npm run build
      env:
        VITE_API_BASE_URL: 'http://54.90.68.15:5000'
  artifacts:
    baseDirectory: frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*

backend:
  phases:
    build:
      commands:
        - echo "Backend build skipped"
